#!/bin/bash
DIR=$PWD/environment

# Docker-compose
docker-compose -f environment/docker-compose.yml --env-file .env.development up -d

# Terraform
[ -e "$DIR/.terraform" ] || $DIR/terraform init
$DIR/terraform apply -auto-approve

echo "\nDevpay dependencies initialization finished 🎉🎉🎉"